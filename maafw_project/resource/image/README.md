# Local recognition resources

Do not commit extracted game assets or private screenshots.

For local development, place manually approved node templates under:

```text
image/nodes/<node-kind>/*.png
image/parts/<part-id>/*.png
```

Every crop should be taken from a lossless screenshot after MaaFramework has
normalized it to 1280x720.
